# miden-build-utils

Build-time helpers shared by the `build.rs` scripts of the Miden workspace crates.

Provides utilities for locating and copying MASM sources, extracting MASM error
constants into generated Rust files, and assembling and writing MAST packages.
