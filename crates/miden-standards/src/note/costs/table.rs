// This file is @generated by `make update-note-costs`. Do not edit by hand.

/// Cycles of the canonical network-account transaction consuming a P2ID note,
/// maximum across the benchmarked execution paths.
pub const P2ID_CONSUMPTION_CYCLES: u64 = 13185;

/// Cycles of the canonical network-account transaction consuming a P2IDE note,
/// maximum across the benchmarked execution paths.
pub const P2IDE_CONSUMPTION_CYCLES: u64 = 13465;

/// Cycles of the canonical network-account transaction consuming a SWAP note,
/// maximum across the benchmarked execution paths.
pub const SWAP_CONSUMPTION_CYCLES: u64 = 16967;

/// Cycles of the canonical network-account transaction consuming a PSWAP note,
/// maximum across the benchmarked execution paths.
pub const PSWAP_CONSUMPTION_CYCLES: u64 = 23248;

/// Cycles of the canonical network-account transaction consuming a MINT note,
/// maximum across the benchmarked execution paths.
pub const MINT_CONSUMPTION_CYCLES: u64 = 28216;

/// Cycles of the canonical network-account transaction consuming a BURN note,
/// maximum across the benchmarked execution paths.
pub const BURN_CONSUMPTION_CYCLES: u64 = 22883;

/// Cycles of the canonical network-account transaction consuming a FAUCET_POLICY_ACTION note,
/// maximum across the benchmarked execution paths.
pub const FAUCET_POLICY_ACTION_CONSUMPTION_CYCLES: u64 = 22260;

/// Cycles of the canonical network-account transaction consuming a PAUSE_ACTION note,
/// maximum across the benchmarked execution paths.
pub const PAUSE_ACTION_CONSUMPTION_CYCLES: u64 = 12255;

/// Cycles of the canonical network-account transaction consuming an OWNER_ACTION note,
/// maximum across the benchmarked execution paths.
pub const OWNER_ACTION_CONSUMPTION_CYCLES: u64 = 11849;

/// Cycles of the canonical network-account transaction consuming an RBAC_ACTION note,
/// maximum across the benchmarked execution paths.
pub const RBAC_ACTION_CONSUMPTION_CYCLES: u64 = 15143;

/// Cycles of the canonical network-account transaction consuming a FEE_SPONSORSHIP note,
/// maximum across the benchmarked execution paths.
pub const FEE_SPONSORSHIP_CONSUMPTION_CYCLES: u64 = 16432;
