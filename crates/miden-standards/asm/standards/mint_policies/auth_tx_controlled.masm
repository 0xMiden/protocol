# POLICY PROCEDURES
# ================================================================================================

#! Dummy auth-tx-controlled mint predicate.
#!
#! Inputs:  [amount, tag, note_type, RECIPIENT, pad(9)]
#! Outputs: [amount, tag, note_type, RECIPIENT, pad(9)]
#! Invocation: dynexec
pub proc auth_tx_controlled
    # Dummy predicate, no checks yet.
    push.0 drop
end
