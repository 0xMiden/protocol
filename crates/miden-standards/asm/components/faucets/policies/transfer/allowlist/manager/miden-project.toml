[package]
name              = "miden-standards-faucets-policies-transfer-allowlist-manager"
version.workspace = true

[lib]
kind      = "account-component"
namespace = "miden::standards::components::faucets::policies::transfer::allowlist::manager"
path      = "manager.masm"

[dependencies]
miden-core.workspace      = true
miden-protocol.workspace  = true
miden-standards.workspace = true

[package.metadata.account-component]
description = "Authority-gated allowlist admin: wraps `allowlist::allow_account` / `disallow_account` with the account-wide Authority component."
name        = "miden::standards::faucets::policies::transfer::allowlist::manager"
version     = "1.0.0"
