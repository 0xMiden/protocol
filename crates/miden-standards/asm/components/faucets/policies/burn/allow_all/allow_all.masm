# The MASM code of the `allow_all` Burn Policy Account Component (auth-controlled family).
#
# Exposes the `check_policy` procedure so its MAST root can be registered as the active
# (or allowed) policy on a `BurnPolicyManager`. Storage-free.

pub use {check_policy} from miden::standards::faucets::policies::burn::allow_all
