[package]
name              = "miden-standards-inspection-code-inspection"
version.workspace = true

[lib]
kind      = "account-component"
namespace = "miden::standards::components::inspection::code_inspection"
path      = "code_inspection.masm"

[dependencies]
miden-core.workspace      = true
miden-protocol.workspace  = true
miden-standards.workspace = true

[package.metadata.account-component]
description = "Read-only introspection over the account's own code"
name        = "miden::standards::inspection::code_inspection"
version     = "1.0.0"
