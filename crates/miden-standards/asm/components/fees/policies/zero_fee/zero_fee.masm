# The MASM code of the `zero_fee` Fee Policy Account Component.
#
# Exposes the `compute_note_fee` procedure so its MAST root can be registered as the active
# (or allowed) fee policy on a `FeeManager`. Storage-free.
#
# See the `ZeroFeePolicy` Rust type's documentation for more details.

pub use {compute_note_fee} from miden::standards::fees::policies::zero_fee
